// Configuration for API endpoints
// Update this URL when you deploy your server
const CONFIG = {
    API_BASE_URL: 'https://invoice-web-niq4.onrender.com/api'
    // For local development, use: 'http://localhost:3000/api'
};

window.CONFIG = CONFIG;
